# Vercel installs this for the Python function in api/index.py. Mirrors
# backend/requirements.txt — that file remains the source of truth for the
# normal deployment; keep the two in sync when a dependency changes.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
pandas==2.2.3
numpy==2.2.1
SQLAlchemy==2.0.36
APScheduler==3.11.0
structlog==24.4.0
PyYAML==6.0.2
